__pycache__/
*.pyc
*.pyo
.pytest_cache/
.DS_Store
.venv/
venv/
.env
.hermes/
*.egg-info/
dist/
build/

# Local runs
dev_run_results/
verify_report.json

# File-based output contract: skill writes here when AIASE_RESULT_PATH is unset (local runs only)
aiase_result.json
*.tmp

# Editor / agent local config (never a submission artifact)
.claude/

# TA file-based update package (reference only; merged into the repo, not committed as-is)
aiase-2026-filebased-update.zip

# Generated dev set sqlite files — students rebuild via dev_set/basic/build_dbs.py
dev_set/basic/dbs/*.sqlite

# Hypothesis property-test cache (regenerated by pytest; never commit)
.hypothesis/

# Course spec document — local reference only, NOT a submission artifact (keep out of the upload)
TAICA2026_FinalProject.html
